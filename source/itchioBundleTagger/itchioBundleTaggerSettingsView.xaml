<UserControl x:Class="itchioBundleTagger.itchioBundleTaggerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <CheckBox IsChecked="{Binding UseTagPrefix}" Content="Prefix tag names with: " VerticalAlignment="Center"/>
            <TextBox Text="{Binding TagPrefix}" Width="200" VerticalAlignment="Center" IsEnabled="{Binding UseTagPrefix}"/>
        </StackPanel>
        <CheckBox IsChecked="{Binding AddFreeTag}" Content="Add 'Free' tag if appliccable" Margin="0,20,0,0"/>
        <CheckBox IsChecked="{Binding AddAvailableOnSteamTag}" Content="Add 'Available on Steam' tag if appliccable" Margin="0,20,0,0"/>
        <CheckBox IsChecked="{Binding AddSteamLink}" Content="Add link to Steam store page if appliccable" Margin="0,20,0,0"/>        
    </StackPanel>
</UserControl>
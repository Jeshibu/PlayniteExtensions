<UserControl x:Class="GOGMetadata.GOGMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:gog="clr-namespace:GOGMetadata"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=gog:GOGMetadataSettingsViewModel}"
             d:DesignHeight="400" d:DesignWidth="600">
    <TabControl>
        <TabItem Header="General">
            <StackPanel Margin="20">
                <CheckBox IsChecked="{Binding Settings.UseVerticalCovers}" Content="Use vertical covers" />
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock VerticalAlignment="Center" Text="Language" Margin="0,0,10,0" />
                    <ComboBox DisplayMemberPath="Name" SelectedValuePath="Slug" IsEditable="false" MinWidth="150"
                              SelectedValue="{Binding Settings.Locale}" ItemsSource="{Binding Path=Languages}" />
                </StackPanel>
                <TextBlock TextWrapping="Wrap" Margin="0 10 0 0">Background image type priority:</TextBlock>
                <ListBox Margin="0 5" Width="210" Height="80" ItemsSource="{Binding Settings.BackgroundTypePriority}" HorizontalAlignment="Left"
                         dd:DragDrop.DragDropContext="Background" dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Width="Auto" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBlock>Drag and drop to reorder. First in the list gets priority.</TextBlock>
                <Button Command="{Binding InitializeCatalogDataCommand}" Content="Test" Margin="20" />
            </StackPanel>
        </TabItem>
        <TabItem Header="Tag Blacklist">
            <DockPanel Margin="20">
                <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" DockPanel.Dock="Top" Text="Excluded tags will not be added to games." />

                <Grid VerticalAlignment="Stretch" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="Left" Width="*" />
                        <ColumnDefinition x:Name="Middle" Width="95" />
                        <ColumnDefinition x:Name="Right" Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="Header" MaxHeight="20" />
                        <RowDefinition x:Name="Body" />
                    </Grid.RowDefinitions>
                    <Grid.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="FontSize" Value="40" />
                            <Setter Property="Margin" Value="20,12" />
                            <Setter Property="Padding" Value="8,2" />
                            <Setter Property="FontFamily" Value="{DynamicResource FontIcoFont}" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                        </Style>
                    </Grid.Resources>
                    <TextBlock HorizontalAlignment="Center" Grid.Column="0" Grid.Row="0" Text="Available tags" />
                    <TextBlock HorizontalAlignment="Center" Grid.Column="2" Grid.Row="0" Text="Excluded tags" />
                    <ListBox DockPanel.Dock="Top" Name="Whitelist"
                             SelectionMode="Extended" MinHeight="220"
                             VerticalAlignment="Stretch"
                             ItemsSource="{Binding OkayTags}"
                             DisplayMemberPath="Name"
                             Grid.Column="0" Grid.Row="1" />
                    <ListBox DockPanel.Dock="Top" Name="Blacklist"
                             SelectionMode="Extended" MinHeight="220"
                             VerticalAlignment="Stretch"
                             ItemsSource="{Binding BlacklistedTags}"
                             DisplayMemberPath="Name"
                             Grid.Column="2" Grid.Row="1" />
                    <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
                        <Button Content="&#xEA94;" Command="{Binding BlacklistCommand}" CommandParameter="{Binding SelectedItems, ElementName=Whitelist}" ToolTip="Add to excluded tags" />
                        <Button Content="&#xEA93;" Command="{Binding WhitelistCommand}" CommandParameter="{Binding SelectedItems, ElementName=Blacklist}" ToolTip="Remove from excluded tags" />
                    </StackPanel>
                </Grid>
            </DockPanel>
        </TabItem>
    </TabControl>

</UserControl>
